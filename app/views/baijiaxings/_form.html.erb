<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@baijiaxing, :html => { :role => "form" }) do |f| %>
        <% if @baijiaxing.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@baijiaxing.errors.count, "error") %> prohibited this baijiaxing from being saved:</h4>

            <ul>
            <% @baijiaxing.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

                  <div class="form-group">
            <%= f.label :name %>
            <%= f.text_field :name, :class => "form-control" %>
          </div>
                  <div class="form-group">
            <%= f.label :author %>
            <%= f.text_field :author, :class => "form-control" %>
          </div>
                  <div class="form-group">
            <%= f.label :source %>
            <%= f.text_area :source, :class => "form-control" %>
          </div>
                  <div class="form-group">
            <%= f.label :celebrity %>
            <%= f.text_area :celebrity, :class => "form-control" %>
          </div>
                  <div class="form-group">
            <%= f.label :distributing %>
            <%= f.text_area :distributing, :class => "form-control" %>
          </div>
                <div class="form-group">
          <%= f.submit :class => "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</section>
