<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.baijiaxing') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.baijiaxing') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Baijiaxing %>
              <%= link_to new_baijiaxing_path(@baijiaxing), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.baijiaxing')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.baijiaxing.id') -%></th>
                                                    <th><%=t('activerecord.attributes.baijiaxing.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.baijiaxing.author') -%></th>
                                                  <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @baijiaxings) do |baijiaxing| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= baijiaxing.name -%></td>
                                                                      <td><%= baijiaxing.author -%></td>
                                                  <% if can? :read, baijiaxing %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{baijiaxing_path(baijiaxing)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, baijiaxing %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_baijiaxing_path(baijiaxing) %></td>
                <% end %>
                <% if can? :destroy, baijiaxing %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), baijiaxing, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @baijiaxings %>
</section>
