<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.caigentan') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.caigentan') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Caigentan %>
              <%= link_to new_caigentan_path(@caigentan), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.caigentan')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.caigentan.id') -%></th>
                                                    <th><%=t('activerecord.attributes.caigentan.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.caigentan.author') -%></th>
                                                                      <th><%=t('activerecord.attributes.caigentan.chapter') -%></th>                                <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @caigentans) do |caigentan| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= caigentan.name -%></td>
                                                                      <td><%= caigentan.author -%></td>
                                                                      <td><%= caigentan.chapter -%></td>
                                                                                                                                                                    <% if can? :read, caigentan %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{caigentan_path(caigentan)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, caigentan %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_caigentan_path(caigentan) %></td>
                <% end %>
                <% if can? :destroy, caigentan %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), caigentan, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @caigentans %>
</section>
