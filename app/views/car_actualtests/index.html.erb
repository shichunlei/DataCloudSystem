<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.car_actualtest') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.car_actualtest') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, CarActualtest %>
              <%= link_to new_car_actualtest_path(@car_actualtest), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.car_actualtest')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                <th><%=t('activerecord.attributes.car_actualtest.id') -%></th>

                <th><%=t('activerecord.attributes.car_actualtest.car_model') -%></th>

                <th><%=t('activerecord.attributes.car_actualtest.accelerationtime100') -%></th>

                <th><%=t('activerecord.attributes.car_actualtest.brakingdistance') -%></th>
                <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @car_actualtests) do |car_actualtest| %>
                <td><%= @page = @page + 1   -%></td>
                <td><%= car_actualtest.car_model.name -%></td>
                <td><%= car_actualtest.accelerationtime100 -%></td>
                <td><%= car_actualtest.brakingdistance -%></td>
                <% if can? :read, car_actualtest %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{car_actualtest_path(car_actualtest)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, car_actualtest %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_car_actualtest_path(car_actualtest) %></td>
                <% end %>
                <% if can? :destroy, car_actualtest %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), car_actualtest, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @car_actualtests %>
</section>
