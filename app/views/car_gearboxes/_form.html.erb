<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@car_gearbox, :html => { :role => "form" }) do |f| %>
        <% if @car_gearbox.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@car_gearbox.errors.count, "error") %> prohibited this car_gearbox from being saved:</h4>

            <ul>
            <% @car_gearbox.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.label :car_model %>
        <%= f.text_field :car_model, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :gearbox %>
        <%= f.text_field :gearbox, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :shiftpaddles %>
        <%= f.text_field :shiftpaddles, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 