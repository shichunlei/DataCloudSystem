<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.fushengliuji') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.fushengliuji') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Fushengliuji %>
              <%= link_to new_fushengliuji_path(@fushengliuji), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.fushengliuji')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.fushengliuji.id') -%></th>
                                                    <th><%=t('activerecord.attributes.fushengliuji.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.fushengliuji.author') -%></th>
                                                                      <th><%=t('activerecord.attributes.fushengliuji.chapter') -%></th>
                                                                                                                                                                    <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @fushengliujis) do |fushengliuji| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= fushengliuji.name -%></td>
                                                                      <td><%= fushengliuji.author -%></td>
                                                                      <td><%= fushengliuji.chapter -%></td>
                                                                                                                                                                    <% if can? :read, fushengliuji %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{fushengliuji_path(fushengliuji)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, fushengliuji %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_fushengliuji_path(fushengliuji) %></td>
                <% end %>
                <% if can? :destroy, fushengliuji %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), fushengliuji, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @fushengliujis %>
</section>
