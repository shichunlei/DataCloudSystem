<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.hanshu') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.hanshu') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Hanshu %>
              <%= link_to new_hanshu_path(@hanshu), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.hanshu')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.hanshu.id') -%></th>
                                                    <th><%=t('activerecord.attributes.hanshu.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.hanshu.chapter') -%></th>
                                                                      <th><%=t('activerecord.attributes.hanshu.author') -%></th>
                                                                                                                                                                    <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 30 -%>
              <%= content_tag_for(:tr, @hanshus) do |hanshu| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= hanshu.name -%></td>
                                                                      <td><%= hanshu.chapter -%></td>
                                                                      <td><%= hanshu.author -%></td>
                                                                                                                                                                    <% if can? :read, hanshu %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{hanshu_path(hanshu)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, hanshu %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_hanshu_path(hanshu) %></td>
                <% end %>
                <% if can? :destroy, hanshu %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), hanshu, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @hanshus %>
</section>
