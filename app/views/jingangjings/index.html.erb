<style media="screen">
  td{
    vertical-align:middle!important;
  }
</style>
<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.jingangjing') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.jingangjing') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Jingangjing %>
              <%= link_to new_jingangjing_path(@jingangjing), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.jingangjing')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.jingangjing.id') -%></th>
                                                                <th><%=t('activerecord.attributes.jingangjing.name') -%></th>
                                                                                                                                                      <th></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @jingangjings) do |jingangjing| %>
                                <td><%= @page = @page + 1   -%></td>
                                                                <td><%= jingangjing.name -%></td>
                                                                                                                                                <% if can? :read, jingangjing %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{jingangjing_path(jingangjing)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, jingangjing %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_jingangjing_path(jingangjing) %></td>
                <% end %>
                <% if can? :destroy, jingangjing %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), jingangjing, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @jingangjings %>
</section>
