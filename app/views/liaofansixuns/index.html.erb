<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.liaofansixun') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.liaofansixun') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Liaofansixun %>
              <%= link_to new_liaofansixun_path(@liaofansixun), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.liaofansixun')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.liaofansixun.id') -%></th>
                                                    <th><%=t('activerecord.attributes.liaofansixun.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.liaofansixun.author') -%></th>
                                                                      <th><%=t('activerecord.attributes.liaofansixun.chapter') -%></th>
                                                                                                                                                                    <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @liaofansixuns) do |liaofansixun| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= liaofansixun.name -%></td>
                                                                      <td><%= liaofansixun.author -%></td>
                                                                      <td><%= liaofansixun.chapter -%></td>
                                                                                                                                                                    <% if can? :read, liaofansixun %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{liaofansixun_path(liaofansixun)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, liaofansixun %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_liaofansixun_path(liaofansixun) %></td>
                <% end %>
                <% if can? :destroy, liaofansixun %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), liaofansixun, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @liaofansixuns %>
</section>
