<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.lvshichunqiu') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.lvshichunqiu') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <!-- <% if can? :create, Lvshichunqiu %>
              <%= link_to new_lvshichunqiu_path(@lvshichunqiu), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.lvshichunqiu')%>
              <% end %>
            <% end %> -->
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.lvshichunqiu.id') -%></th>

                              <th><%=t('activerecord.attributes.lvshichunqiu.name') -%></th>

                              <th><%=t('activerecord.attributes.lvshichunqiu.chapter') -%></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @lvshichunqius) do |lvshichunqiu| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= lvshichunqiu.name -%></td>


                  <td><%= lvshichunqiu.chapter -%></td>
                                                      <% if can? :read, lvshichunqiu %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{lvshichunqiu_path(lvshichunqiu)}?page=#{current_page}" %></td>
                <% end %>
                <!-- <% if can? :edit, lvshichunqiu %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_lvshichunqiu_path(lvshichunqiu) %></td>
                <% end %>
                <% if can? :destroy, lvshichunqiu %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), lvshichunqiu, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %> -->
                <td></td>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @lvshichunqius %>
</section>
