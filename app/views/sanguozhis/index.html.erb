<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.sanguozhi') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.sanguozhi') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Sanguozhi %>
              <%= link_to new_sanguozhi_path(@sanguozhi), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.sanguozhi')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.sanguozhi.id') -%></th>

                              <th><%=t('activerecord.attributes.sanguozhi.name') -%></th>

                              <th><%=t('activerecord.attributes.sanguozhi.chapter') -%></th>
                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @sanguozhis) do |sanguozhi| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= sanguozhi.name -%></td>


                  <td><%= sanguozhi.chapter -%></td>
                                                      <% if can? :read, sanguozhi %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{sanguozhi_path(sanguozhi)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, sanguozhi %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_sanguozhi_path(sanguozhi) %></td>
                <% end %>
                <% if can? :destroy, sanguozhi %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), sanguozhi, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @sanguozhis %>
</section>
