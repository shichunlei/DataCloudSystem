<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.sanzijing') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.sanzijing') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Sanzijing %>
              <%= link_to new_sanzijing_path(@sanzijing), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.sanzijing')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.sanzijing.id') -%></th>
                                                    <th><%=t('activerecord.attributes.sanzijing.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.sanzijing.author') -%></th>
                                                                                                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @sanzijings) do |sanzijing| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= sanzijing.name -%></td>
                                                                      <td><%= sanzijing.author -%></td>
                                                                                                                              <% if can? :read, sanzijing %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{sanzijing_path(sanzijing)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, sanzijing %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_sanzijing_path(sanzijing) %></td>
                <% end %>
                <% if can? :destroy, sanzijing %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), sanzijing, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @sanzijings %>
</section>
