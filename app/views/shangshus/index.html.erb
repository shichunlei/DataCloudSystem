<style media="screen">
  td{
    vertical-align:middle!important;
  }
</style>
<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.shangshu') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.shangshu') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Shangshu %>
              <%= link_to new_shangshu_path(@shangshu), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.shangshu')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.shangshu.id') -%></th>
                                                <th><%=t('activerecord.attributes.shangshu.chapter') -%></th>
                                                                <th><%=t('activerecord.attributes.shangshu.name') -%></th>
                                                                <th><%=t('activerecord.attributes.shangshu.author') -%></th>
                                                                <th><%=t('activerecord.attributes.shangshu.content') -%></th>
                                                                <th><%=t('activerecord.attributes.shangshu.commentary') -%></th>
                                                                                                                                                      <th></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @shangshus) do |shangshu| %>
                                <td><%= @page = @page + 1   -%></td>
                                                <td><%= shangshu.chapter -%></td>
                                                                <td><%= shangshu.name -%></td>
                                                                <td><%= shangshu.author -%></td>
                                                                <td><%= shangshu.content -%></td>
                                                                <td><%= shangshu.commentary -%></td>
                                                                                                                                                <% if can? :read, shangshu %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{shangshu_path(shangshu)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, shangshu %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_shangshu_path(shangshu) %></td>
                <% end %>
                <% if can? :destroy, shangshu %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), shangshu, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @shangshus %>
</section>
