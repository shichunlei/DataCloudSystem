<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.shanhaijing') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.shanhaijing') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Shanhaijing %>
              <%= link_to new_shanhaijing_path(@shanhaijing), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.shanhaijing')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.shanhaijing.id') -%></th>

                              <th><%=t('activerecord.attributes.shanhaijing.name') -%></th>

                              <th><%=t('activerecord.attributes.shanhaijing.chapter') -%></th>
                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @shanhaijings) do |shanhaijing| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= shanhaijing.name -%></td>


                  <td><%= shanhaijing.chapter -%></td>
                                                      <% if can? :read, shanhaijing %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{shanhaijing_path(shanhaijing)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, shanhaijing %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_shanhaijing_path(shanhaijing) %></td>
                <% end %>
                <% if can? :destroy, shanhaijing %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), shanhaijing, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @shanhaijings %>
</section>
