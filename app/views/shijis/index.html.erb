<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.shiji') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.shiji') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Shiji %>
              <%= link_to new_shiji_path(@shiji), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.shiji')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.shiji.id') -%></th>

                              <th><%=t('activerecord.attributes.shiji.name') -%></th>

                              <th><%=t('activerecord.attributes.shiji.chapter') -%></th>
                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @shijis) do |shiji| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= shiji.name -%></td>


                  <td><%= shiji.chapter -%></td>
                                                      <% if can? :read, shiji %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{shiji_path(shiji)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, shiji %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_shiji_path(shiji) %></td>
                <% end %>
                <% if can? :destroy, shiji %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), shiji, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @shijis %>
</section>
