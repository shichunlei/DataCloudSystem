<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.soushenji') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.soushenji') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Soushenji %>
              <%= link_to new_soushenji_path(@soushenji), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.soushenji')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.soushenji.id') -%></th>

                              <th><%=t('activerecord.attributes.soushenji.name') -%></th>
                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @soushenjis) do |soushenji| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= soushenji.name -%></td>
                                                      <% if can? :read, soushenji %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{soushenji_path(soushenji)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, soushenji %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_soushenji_path(soushenji) %></td>
                <% end %>
                <% if can? :destroy, soushenji %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), soushenji, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @soushenjis %>
</section>
