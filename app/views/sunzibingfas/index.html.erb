<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.sunzibingfa') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.sunzibingfa') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Sunzibingfa %>
              <%= link_to new_sunzibingfa_path(@sunzibingfa), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.sunzibingfa')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                            <th><%=t('activerecord.attributes.sunzibingfa.id') -%></th>

                              <th><%=t('activerecord.attributes.sunzibingfa.name') -%></th>
                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @sunzibingfas) do |sunzibingfa| %>
                                <td><%= @page = @page + 1   -%></td>


                  <td><%= sunzibingfa.name -%></td>
                                                      <% if can? :read, sunzibingfa %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{sunzibingfa_path(sunzibingfa)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, sunzibingfa %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_sunzibingfa_path(sunzibingfa) %></td>
                <% end %>
                <% if can? :destroy, sunzibingfa %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), sunzibingfa, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>

              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
    <%= paginate @sunzibingfas %>
</section>
