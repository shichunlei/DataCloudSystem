<style media="screen">
  td{
    vertical-align:middle!important;
  }
</style>
<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.wenchang') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.wenchang') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Wenchang %>
              <%= link_to new_wenchang_path(@wenchang), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.wenchang')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.wenchang.id') -%></th>
                                                <th><%=t('activerecord.attributes.wenchang.name') -%></th>
                                                                <th><%=t('activerecord.attributes.wenchang.author') -%></th>
                                                                <th><%=t('activerecord.attributes.wenchang.content') -%></th>
                                                                <th><%=t('activerecord.attributes.wenchang.commentary') -%></th>
                                                                <th><%=t('activerecord.attributes.wenchang.translation') -%></th>
                                                                                                                    <th></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @wenchangs) do |wenchang| %>
                                <td><%= @page = @page + 1   -%></td>
                                                <td><%= wenchang.name -%></td>
                                                                <td><%= wenchang.author -%></td>
                                                                <td><%= wenchang.content -%></td>
                                                                <td><%= wenchang.commentary -%></td>
                                                                <td><%= wenchang.translation -%></td>
                                                                                                                <% if can? :read, wenchang %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{wenchang_path(wenchang)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, wenchang %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_wenchang_path(wenchang) %></td>
                <% end %>
                <% if can? :destroy, wenchang %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), wenchang, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @wenchangs %>
</section>
