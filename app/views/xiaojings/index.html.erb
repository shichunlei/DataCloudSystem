<style media="screen">
  td{
    vertical-align:middle!important;
  }
</style>
<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.xiaojing') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.xiaojing') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Xiaojing %>
              <%= link_to new_xiaojing_path(@xiaojing), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.xiaojing')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.xiaojing.id') -%></th>
                                                <th><%=t('activerecord.attributes.xiaojing.name') -%></th>
                                                                <th><%=t('activerecord.attributes.xiaojing.author') -%></th>
                                                                <th><%=t('activerecord.attributes.xiaojing.content') -%></th>
                                                                <th><%=t('activerecord.attributes.xiaojing.commentary') -%></th>
                                                                <th><%=t('activerecord.attributes.xiaojing.translation') -%></th>
                                                                                                                    <th></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @xiaojings) do |xiaojing| %>
                                <td><%= @page = @page + 1   -%></td>
                                                <td><%= xiaojing.name -%></td>
                                                                <td><%= xiaojing.author -%></td>
                                                                <td><%= xiaojing.content -%></td>
                                                                <td><%= xiaojing.commentary -%></td>
                                                                <td><%= xiaojing.translation -%></td>
                                                                                                                <% if can? :read, xiaojing %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{xiaojing_path(xiaojing)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, xiaojing %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_xiaojing_path(xiaojing) %></td>
                <% end %>
                <% if can? :destroy, xiaojing %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), xiaojing, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @xiaojings %>
</section>
