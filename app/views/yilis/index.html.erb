<style media="screen">
  td{
    vertical-align:middle!important;
  }
</style>
<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.yili') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.yili') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Yili %>
              <%= link_to new_yili_path(@yili), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.yili')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.yili.id') -%></th>
                                                <th><%=t('activerecord.attributes.yili.name') -%></th>
                                                                <th><%=t('activerecord.attributes.yili.author') -%></th>
                                                                <th><%=t('activerecord.attributes.yili.content') -%></th>
                                                                <th><%=t('activerecord.attributes.yili.commentary') -%></th>
                                                                <th><%=t('activerecord.attributes.yili.translation') -%></th>
                                                                                                                    <th></th>
                <th>操作</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @yilis) do |yili| %>
                                <td><%= @page = @page + 1   -%></td>
                                                <td><%= yili.name -%></td>
                                                                <td><%= yili.author -%></td>
                                                                <td><%= yili.content -%></td>
                                                                <td><%= yili.commentary -%></td>
                                                                <td><%= yili.translation -%></td>
                                                                                                                <% if can? :read, yili %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{yili_path(yili)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, yili %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_yili_path(yili) %></td>
                <% end %>
                <% if can? :destroy, yili %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), yili, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @yilis %>
</section>
