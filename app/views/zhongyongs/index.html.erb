<% content_for :content_header  do %>
  <section class="content-header">
    <h1><%=t('views.list')%> <small><%= t('activerecord.models.zhongyong') %></small></h1>

    <ol class="breadcrumb">
      <li><a href="/"><i class="fa fa-dashboard"></i> <%=t('views.home')%></a></li>
      <li class="active"><%= t('activerecord.models.zhongyong') %></li>
    </ol>
  </section>
<% end %>

<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="box box-primary">
        <div class="box-header">
          <div class="box-tools pull-right">
            <% if can? :create, Zhongyong %>
              <%= link_to new_zhongyong_path(@zhongyong), :class => 'btn btn-success' do %>
                <span class="glyphicon glyphicon-plus"></span>
                <%=t('views.create')%><%=t('activerecord.models.zhongyong')%>
              <% end %>
            <% end %>
          </div>
        </div>
        <div class="box-body table-responsive">
          <table class="table table-hover">
            <thead>
              <tr>
                                <th><%=t('activerecord.attributes.zhongyong.id') -%></th>
                                                    <th><%=t('activerecord.attributes.zhongyong.name') -%></th>
                                                                      <th><%=t('activerecord.attributes.zhongyong.author') -%></th>
                                                                      <th><%=t('activerecord.attributes.zhongyong.content') -%></th>
                                                                      <th><%=t('activerecord.attributes.zhongyong.commentary') -%></th>
                                                                      <th><%=t('activerecord.attributes.zhongyong.translation') -%></th>
                                                                                                                              <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% current_page = @page.nil? ? 1 : @page -%>
              <% @page = @page.nil? ? 0 : (@page.to_i - 1) * 10 -%>
              <%= content_tag_for(:tr, @zhongyongs) do |zhongyong| %>
                                <td><%= @page = @page + 1   -%></td>
                                                    <td><%= zhongyong.name -%></td>
                                                                      <td><%= zhongyong.author -%></td>
                                                                      <td><%= zhongyong.content -%></td>
                                                                      <td><%= zhongyong.commentary -%></td>
                                                                      <td><%= zhongyong.translation -%></td>
                                                                                                                              <% if can? :read, zhongyong %>
                  <td class="list-opertion"><%= link_to t('views.show'), "#{zhongyong_path(zhongyong)}?page=#{current_page}" %></td>
                <% end %>
                <% if can? :edit, zhongyong %>
                  <td class="list-opertion"><%= link_to t('views.edit'), edit_zhongyong_path(zhongyong) %></td>
                <% end %>
                <% if can? :destroy, zhongyong %>
                  <td class="list-opertion"><%= link_to t('views.destroy'), zhongyong, method: :delete, data: { confirm: t('views.confirm') } %></td>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <%= paginate @zhongyongs %>
</section>
